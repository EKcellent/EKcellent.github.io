# Esha Kapoor Portfolio

Static GitHub Pages site.

## Deploy

Upload `index.html` and `styles.css` to the root of `EKcellent.github.io`.
Then set GitHub Pages to deploy from `main` branch, root folder.
